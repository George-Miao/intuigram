# Roadmap

## Priorities

- **p-core:** This work is necessary for the first daily-drivable PoC.
- **p-high:** This work is necessary before Intuigram can replace a primary Telegram client.
- **p-mid:** This work is useful after the first PoC.
- **p-low:** This work is in the deferred backlog. It has no near-term promise.

## TODO

- [ ] **p-core:** Make Folder and Chat switching render immediately while Telegram, storage, media, or synchronization work is pending. Never wait for background work on the UI path. Coalesce or cancel stale navigation loads. Give priority to the newest selection. Add a regression scenario that proves repeated rapid switches continue to accept input and draw loading-state frames without stalls of multiple seconds.
- [ ] **p-core:** Measure and reduce CPU use during idle operation and navigation. Remove busy polling, unnecessary full redraws, and redundant background wakeups. Use a reproducible same-machine benchmark for idle operation and rapid navigation. Keep Intuigram CPU use below Telegram Swift CPU use. Preserve responsive input and loading animations.
- [ ] **p-core:** Provide bounded concurrent live MTProto invocation through the Account session without reducing single ownership. Correlate out-of-order replies by request identity. Keep passive updates in progress. Isolate flood waits and cancellation for each invocation. Use deterministic transport tests for capacity, failure, and shutdown behavior.
- [ ] **p-core:** Add end-to-end IPv6 support to the MTProto network stack. Resolve and connect to IPv6 Telegram endpoints and proxy servers. Support IPv6 destinations through SOCKS5, HTTP CONNECT, and MTProxy. Preserve IPv4 support and deterministic route fallback. Add hermetic direct and proxy transport tests for IPv6 literals, DNS results, fallback, and failure reporting.
- [ ] **p-core:** Replace the vendored `crates/compio-actor` package with the actor API in the upstream Compio `master` branch. Remove the vendored workspace member and standalone dependency. Move imports and feature selection to upstream Compio. Refresh dependency resolution. Keep the Account actor lifecycle, backpressure, cancellation, and shutdown tests passing.
- [ ] **p-core:** Remove SNAFU from the reusable `compio-term` crate. Return `std::io::Error` directly from its fallible public interfaces. Delete the crate-local error wrappers and dependency. Preserve useful OS error information. Translate terminal failures at the Intuigram application boundary. Keep the Unix and Windows backend tests passing.
- [ ] **p-core:** Put each dependency that a workspace crate uses in the root `[workspace.dependencies]` table. Reference each dependency from member manifests with `workspace = true`. Include normal, development, build, target-specific, path, and Git dependencies. Preserve the required features of each crate. Keep Cargo metadata and the full workspace checks clean.
- [ ] **p-core:** Split Telegram application effects into serialized control, small-media, and large-transfer lanes. Replace the global Telegram effect slot and the single active avatar and preview slots with bounded correlated work. Permit visible media to complete out of order without stale state. Add a hermetic scenario that proves held media does not block Chat navigation or independent Telegram work.
- [ ] **p-core:** Reuse persistent Account-owned media sessions for each data center. Keep ordinary RPCs and updates separate from file traffic. Import cross-DC authorization one time for each retained session instead of one time for each asset. Use media-only DC endpoints when Telegram advertises them. Shut down or rebuild all sessions as one coherent operation after reconnect.
- [ ] **p-core:** Schedule Telegram media with explicit per-DC backpressure and priority. Give visible previews and avatars priority over background prefetch. Use the Telegram small-operation and large-operation limits with safe defaults. Cancel or coalesce stale viewport work. Bound the number of concurrent files and the number of in-flight bytes. Do not starve updates or user actions.
- [ ] **p-core:** Download Telegram files through reusable normalized locations without another request for their Messages. Store sufficient Intuigram-owned media identity to select the intended thumbnail. Refresh expired file references. Fetch aligned parts concurrently and assemble them in order. Support file migration, CDN redirects, decryption, and hash verification.
- [x] **p-core:** Make a native clipboard image paste into the Composer create a sendable image attachment. Keep existing Draft text as the caption. Show the staged attachment in the Composer context. Report unsupported or unreadable clipboard content without clearing the Draft. Use hermetic adapter tests for clipboard-format priority and the paste-to-send procedure.
- [ ] **p-core:** Change each flat source-file family with the same prefix into a cohesive directory module with a small owning interface. Do not use file-name prefixes to simulate grouping. Audit the full workspace, including the `intuigram-telegram` `client_*` family. Put related implementations under normal `mod.rs` boundaries without a behavior change.
- [ ] **p-core:** Move the contents of `crates/intuigram-telegram/src/source/` directly into `crates/intuigram-telegram/src/`. Move its files and cohesive child modules directly under the crate root. Remove the redundant `source` wrapper and transitional re-exports. Preserve module privacy and behavior. Keep the Telegram crate and workspace checks clean.
- [ ] **p-core:** Change the executable composition crates so that `crates/intuigram` owns all current `intuigram-app` content except the `src/application` implementation. Move each former `crates/intuigram-app/src/application/*` module directly into `crates/intuigram-app/src/*`. Move affected tests and dependencies to their new owners. Remove transitional wrapper modules. Preserve behavior without a package dependency cycle. Update the documented crate layout.
- [ ] **p-high:** Make keyboard bindings configurable through a Figment-based configuration system. Support layered TOML, YAML, JSON, environment, and command-line sources. Keep a complete default keymap. Generate the Action Bar and Help from the active configuration.
- [ ] **p-high:** Make the terminal palette configurable. Keep Everforest Light as a built-in bright theme. Add automatic detection of a light or dark terminal background. Provide an explicit configuration override.
- [ ] **p-high:** Add sender, date-range, and media-type filters to Chat Search and Global Search.
- [ ] **p-high:** Measure image decoding, resizing, Unicode avatar fallback tiles, and terminal-protocol encoding in image-heavy Transcripts. Move measured blocking CPU work to `spawn_blocking` or the dedicated graphics worker. Preserve ordered placements and responsive input.
- [ ] **p-high:** Add a dedicated Global Search shortcut. Use `Ctrl+Shift+F` when enhanced keyboard input can distinguish it from `Ctrl+F`. Use `Ctrl+K` on legacy terminals. Show only the effective binding in the Action Bar and Help.
- [ ] **p-high:** Add Telegram call support. Until this work is complete, calls stay outside the Daily Driver product promise.
- [ ] **p-high:** Get the current location from platform services or a configured provider. Broadcast live locations. Never use IP geolocation without an explicit user action.
- [ ] **p-high:** Investigate Secret Chat feasibility and security requirements without an implementation promise. Each future proposal must include the complete end-to-end protocol, device-bound key lifecycle, durable encrypted state, sequence validation, self-destruct behavior, interoperability, and an independent security review.
- [ ] **p-high:** Keep one blank terminal row between the Folder strip and the Action Bar. Remove the extra vertical gap without changing either region's content or behavior.
- [ ] **p-high:** Add playback for Telegram audio files and voice messages. Provide play, pause, seek, progress, and stop controls. Keep terminal input and rendering responsive during playback.
