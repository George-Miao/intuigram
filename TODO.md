# Roadmap

## Priorities

- **p-core:** Required for the first daily-drivable PoC.
- **p-high:** Required before Intuigram can reasonably replace a primary Telegram client.
- **p-mid:** Valuable follow-up after the first PoC.
- **p-low:** Deferred backlog with no near-term promise.

## TODO

- [ ] **p-core:** Make Folder and Chat switching render immediately while Telegram, storage, media, or synchronization work is pending. Never await background work on the UI path; coalesce or cancel stale navigation loads, prioritize the newest selection, and add a regression scenario proving repeated rapid switches keep accepting input and drawing loading-state frames without multi-second stalls.
- [ ] **p-core:** Profile and reduce idle and navigation CPU usage. Eliminate busy polling, unnecessary full redraws, and redundant background wakeups; under a reproducible same-machine idle and rapid-navigation benchmark, keep Intuigram below Telegram Swift's CPU consumption while preserving responsive input and loading animations.
- [ ] **p-core:** Expose bounded concurrent live MTProto invocation through the Account session without weakening single ownership. Correlate out-of-order replies by request identity, keep passive updates progressing, isolate flood waits and cancellation per invocation, and cover capacity, failure, and shutdown behavior with deterministic transport tests.
- [ ] **p-core:** Replace the vendored `crates/compio-actor` package with the actor API merged into Compio's upstream `master` branch. Remove the vendored workspace member and standalone dependency, migrate imports and feature selection to upstream Compio, refresh dependency resolution, and keep the Account actor lifecycle, backpressure, cancellation, and shutdown tests passing.
- [ ] **p-core:** Remove SNAFU from the reusable `compio-term` crate and expose `std::io::Error` directly from its fallible public interfaces. Delete the crate-local error wrappers and dependency, preserve useful OS error information, translate terminal failures at the Intuigram application boundary, and keep Unix and Windows backend tests passing.
- [ ] **p-core:** Centralize every dependency used by a workspace crate under the root `[workspace.dependencies]` table and reference it from member manifests with `workspace = true`. Cover normal, development, build, target-specific, path, and Git dependencies while preserving each crate's required features and keeping Cargo metadata plus the full workspace checks clean.
- [ ] **p-core:** Split Telegram application effects into serialized control, small-media, and large-transfer lanes. Replace the global Telegram effect slot and single active avatar/preview slots with bounded correlated work, allow visible media to complete out of order without stale state, and add a hermetic scenario proving held media does not block Chat navigation or independent Telegram work.
- [ ] **p-core:** Reuse persistent Account-owned media sessions per data center. Keep ordinary RPCs and updates separate from file traffic, import cross-DC authorization once per retained session instead of once per asset, route through media-only DC endpoints where advertised, and shut down or rebuild every session coherently on reconnect.
- [ ] **p-core:** Schedule Telegram media with explicit per-DC backpressure and priority. Admit visible previews and avatars ahead of background prefetch, use Telegram's small/large active-operation limits with safe defaults, cancel or coalesce stale viewport work, and bound both concurrent files and in-flight bytes without starving updates or user actions.
- [ ] **p-core:** Download Telegram files through reusable normalized locations without refetching their Messages. Persist enough Intuigram-owned media identity to select the intended thumbnail, refresh expired file references, fetch aligned parts concurrently with ordered assembly, and support file migration plus CDN redirect, decryption, and hash verification.
- [ ] **p-core:** Make pasting an image from the native clipboard into the Composer create a sendable image attachment. Preserve any existing draft text as the caption, show the staged attachment in Composer context, report unsupported or unreadable clipboard content without clearing the draft, and cover clipboard-format precedence plus the paste-to-send flow with hermetic adapter tests.
- [ ] **p-core:** Refactor every flat same-prefix source family into a cohesive directory module with a small owning interface. Do not use filename prefixes to simulate grouping; audit the full workspace, including the `intuigram-telegram` `client_*` family, and organize related implementation beneath ordinary `mod.rs` boundaries without changing behavior.
- [ ] **p-core:** Flatten `crates/intuigram-telegram/src/source/` into `crates/intuigram-telegram/src/`. Move its files and cohesive child modules directly beneath the crate root, remove the redundant `source` wrapper and transitional re-exports, preserve module privacy and behavior, and keep the Telegram crate plus workspace checks clean.
- [ ] **p-core:** Reshape the executable composition crates so `crates/intuigram` owns everything currently in `intuigram-app` except the `src/application` implementation, and flatten every former `crates/intuigram-app/src/application/*` module into `crates/intuigram-app/src/*`. Move the affected tests and dependencies to their new owners, eliminate transitional wrapper modules, preserve behavior without introducing a package dependency cycle, and update the documented crate layout to match.
- [ ] **p-high:** Make keyboard bindings configurable through a Figment-based configuration system. Support layered TOML, YAML, JSON, environment, and command-line sources; keep a complete default keymap; and generate the on-screen Action Bar and Help from the active configuration.
- [ ] **p-high:** Make the terminal palette configurable, keep Everforest Light as a built-in bright theme, and add automatic light/dark terminal-background detection with an explicit configuration override.
- [ ] **p-high:** Add sender, date-range, and media-type filters to Chat Search and Global Search.
- [ ] **p-high:** Profile image decoding, resizing, Unicode avatar fallback tiles, and terminal-protocol encoding under image-heavy Transcripts; move any measured blocking CPU work to `spawn_blocking` or the dedicated graphics worker while preserving ordered placements and responsive input.
- [ ] **p-high:** Add a dedicated Global Search shortcut. Use `Ctrl+Shift+F` when enhanced keyboard input can distinguish it from `Ctrl+F`, fall back to `Ctrl+K` on legacy terminals, and show only the effective binding in the Action Bar and Help.
- [ ] **p-high:** Add Telegram call support. Until then, calls are explicitly outside the Daily Driver product promise.
- [ ] **p-high:** Obtain current location from platform services or a configured provider and broadcast live locations. Never use IP geolocation silently.
- [ ] **p-high:** Investigate Secret Chat feasibility and security requirements without promising implementation. Any future proposal must cover the complete end-to-end protocol, device-bound key lifecycle, durable encrypted state, sequence validation, self-destruct behavior, interoperability, and independent security review.
